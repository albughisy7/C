#include <stdio.h>

int main()
{
    system("cls");
    int a = 7;
    int b = 9;

    printf("OPERATOR BITWISE SHR\n\n");

    printf("A = %d\n\n", a);

    // printf("Bitwise OR (A | B) : %d\n\n", a | b);
    // printf("Bitwise AND (A & B) : %d\n\n", a & b);
    // printf("Bitwise XOR (A ^ B) : %d\n\n", a ^ b);
    // printf("Bitwise (~ A) : %d\n", ~a);
    // printf("Bitwise SHL (A << 2) : %d\n", a << 2);
    printf("Bitwise SHR (A >> 2) : %d\n\n", a >> 2);

    // printf("Bitwise NOT (~B) : %d\n\n", ~b);
}